{
  "name": "happy-yoga-web",
  "private": true,
  "dependencies": {
    "eslint": "^4.3.0",
    "faucet-pipeline-js": "^0.12.2",
    "standard": "^10.0.2"
  },
  "scripts": {
    "build:js": "faucet --no-fingerprint --compact",
    "build:js:watch": "npm run build:js -- --watch",
    "test": "eslint --cache *.js src && echo ✓",
    "start": "npm run build:js"
  }
}
